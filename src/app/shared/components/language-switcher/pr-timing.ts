/** Duración de la fase "checks…" antes de pasar a "merged". */
export const CHECKS_MS = 650;

/** Instante en el que la insignia del PR desaparece, contado desde el click. */
export const MERGED_MS = 1500;
